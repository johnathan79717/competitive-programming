#include <iostream>
#include <vector>
using namespace std;

int main() {
  int N;
  cin >> N;
  while(N--) {
    int R, T;
    cin >> R >> T;
    while(T--) {
      int overtake, normal;
      cin >> overtake >> normal;
        
    }
  }
}