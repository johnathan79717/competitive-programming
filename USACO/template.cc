/*
ID: johnath2
PROG: crypt1
LANG: C++11
*/
#include <unordered_set>
#include <unordered_map>
#define NAME "crypt1"

int main() {
#ifndef STDIO
    freopen(NAME".in", "r", stdin);
    freopen(NAME".out", "w", stdout);
#endif
    
    return 0;
}
