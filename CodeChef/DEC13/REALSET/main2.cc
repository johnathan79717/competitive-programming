#include <cstdio>

#define MAX_N 32768;
typedef long long ll;

int A[MAX_N];

class Poly {
public:
  
private:

};

int main() {
  int T;
  scanf("%d", &T);
  while(T--) {
    int N;
    scanf("%d", &N);
    for(int i = 0; i < N; i++)
      scanf("%d", A + i);
  }
}