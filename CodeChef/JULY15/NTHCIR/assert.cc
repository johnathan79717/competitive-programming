#include <string>
#include <vector>
#include <climits>
#include <cstring>
#include <map>
#include <queue>
#include <algorithm>
#include <iomanip>
#include <cmath>
#include <cstdio>
#include <iostream>
#include <set>
#include <deque>
#include <stack>
#include <cassert>
using namespace std;

int main() {
    int T, N, P, M, B, R1, R2, R3, R4;
    cin >> T >> N >> P >> M >> B >> R1 >> R2 >> R3 >> R4;
    assert(R1 >= 1 && R2 >= 1 && R3 >= 1 && R4 >= 1);
    return 0;
}