T = int(input())
for _ in range(T):
    # _, K = map(int, input().split())
    input()

    # A = [int(x) for x in input().split()]
    input()
    # k = 0
    # ans = 0
    # for a in A:
    #     assert(K > 0)
    #     if k >= a:
    #         k -= a
    #     else:
    #         a -= k
    #         k = 0
    #         ans += a // K
    #         a %= K
    #         if a > 0:
    #             ans += 1
    #             k = K - a
    #     if k > 0: k -= 1
    #     # print(ans, k)
    # print(ans)